<%= form_with(model: aluno) do |form| %>
  <% if aluno.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(aluno.errors.count, "error") %> prohibited this aluno from being saved:</h2>

      <ul>
        <% aluno.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nome, style: "display: block" %>
    <%= form.text_field :nome %>
  </div>

  <div>
    <%= form.label :turma, style: "display: block" %>
    <%= form.text_field :turma %>
  </div>

  <div>
    <%= form.label :casa_id, style: "display: block" %>
    <%= form.text_field :casa_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
